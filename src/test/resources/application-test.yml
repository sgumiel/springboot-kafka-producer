spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest

topics:
  pelicula: topic-pelicula-test

kafka:
  consumer:
    group-id: consumer-pelicula-test
